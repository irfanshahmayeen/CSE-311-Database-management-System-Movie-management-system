<?php
/*
include '../connection.php';

// Get data from AJAX request
$foodID = $_POST['foodID'];
$quantity = $_POST['quantity'];
$bookingTime = $_POST['bookingTime'];

// SQL query to insert into foodBookings table
$sql = "INSERT INTO foodBookings (FoodID, Quantity, bookingTime) VALUES ($foodID, $quantity, '$bookingTime')";

// Execute SQL query
if ($conn->query($sql) === TRUE) {
    echo "Food item inserted into cart successfully";
} else {
    echo "Error: " . $sql . "<br>" . $conn->error;
}

// Close connection
$conn->close();
*/
?>